// Create a function that turns positive numbers 
// into negative numbers
//negative numbers to positive
//Opposites!

//function name(parameter){
    //if number is more than or equal to zero (positive)
    //return -Math.abs(num);
    //else if number is less than 0 (negative number)
    //return Math.abs(num)

//}

//Math =object abs= opposite

function opposite(num) {
  if (num >= 0){
    return -Math.abs(num);//your code here
}else{
  return Math.abs(num);
  }
}